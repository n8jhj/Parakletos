class Tile(object):
    def __init__(name):
        """
        PROPERTIES:
        name: name of the type of self
        board: Board self is in
        coord: grid location - (x,y)
        img: image file
        moveable: whether Charactors can move here
        piece: Piece self contains
        """

    def inMoveRange(Charactor):
        """
        sthg like: return whether abs(delta-x) + abs(delta-y) <=
            Charactor.spd
        """
